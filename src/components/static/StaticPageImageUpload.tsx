import { useState } from "react";
import { Button } from "../ui/button";
import { Image } from "lucide-react";
import { FileUpload } from "../FileUpload";
import { toast } from "sonner";

// We'll keep this component but make it empty since we're removing the feature
export function StaticPageImageUpload({ featuredImage, onImageUpload }: any) {
  return null;
}
