
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'

// This is handled by vike-react, so we don't need to import the app here
// The rendering is handled by vike's client-side routing
